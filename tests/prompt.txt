请输出一个重新设计input和passage-input的页面的最小可实现变动的方案，也就是，用人话描述“你要把哪里的什么改成啥样”。
具体要求如下，请逐条照做：
- 将配色变成现代化的蓝白色搭配。
- 润色input页面单词输入框的悬停和聚焦动画。
- 更改input页面“清空列表”“保存列表”按钮的颜色
- 更改input页面通知的颜色（注意通知是JavaScript动态生成的）
- 更改input页面表格的颜色
- 更改passage input页面的背景配色、单词高亮配色
- 增加passage input页面的高亮单词鼠标悬浮效果
（注意高亮单词是JavaScript动态生成的）
- 润色passage input页面文章输入框的悬停和聚焦动画。

禁止做的事情：
- 更改JavaScript文件（动态生成元素时指定class的代码除外）
- 大幅更改HTML结构
- 使用过于复杂的css动画（如果有需求，请使用Web Animation API，经过我的审核后加入代码）
- 覆写innerHTML

必须做的事情：
- 遵守现在的OOP代码风格，以传参的形式获取其他模块的实例

使用十六进制颜色时，请用括号描述其颜色！例如#2BC016（鲜艳的绿色）
回复格式用 Markdown 分节，待我审核后才允许更改代码！

请输出一个完善passage-input功能的最小可实现变动的方案，也就是，用人话描述“你要把哪里的什么改成啥样”。
我作为审核人员，预期是：
- 在input页面新增一个按钮，内部文字为“文章录入”
- 用户悬浮在“文章录入”按钮时，弹出一个信息提示（信息提示的左下角要跟随鼠标移动），内容是“粘贴文章：点一下，就录入”！下面附带"根目录\static\images\passage-input-information.gif"动图（800px宽600px高）补充信息。
- 点击“文章录入”按钮时，用open函数打开一个passage-input页面，使其与input页面建立连接。
- 给passage-input页面的所有单词span（技术上讲就是classname为“passage-span passage-span-highlight”的span元素）添加事件监视器：当被点击时，使用postMessage通知input页面被点击的单词。
- 在input页面的UserEventHandler类中新增listenPassageInput方法，监听passage-input页面传递的信息并录入单词。录入成功时弹出通知：“已录入文章中的xxx单词！”

回复格式用 Markdown 分节，待我审核后才允许更改代码！

现在，请你重新设计input页面的响应式设计，同时加入一个功能：左边框（className为'left'）可以折叠。
我先说响应式设计的要求：
left左边框宽度400px，绝对定位，固定在最左侧（已完成）
content左margin400px（不然会被left挡住），并占据右边所有剩余的空间。
如果页面宽度过小，就自动折叠left左边框；如果页面宽度又变大，就展开左边框。

说到折叠左边框，这也是我要加入的新功能。
具体来说，我要求在折叠的时候：
left左边框播放动画，向左滑动340px（使用transform属性，同时保持绝对定位）；如此同时，classname为content的元素的左margin从400px逐步降低到60px。要求左边框的滑动动画和content的左margin变化进度必须同步，因为这才能带来最佳的视觉效果！
此外，在left左边框向外滑动的同时，它的所有子元素都播放动画，缩小为原来的30%。
整个动画用时350ms。
注意，这个动画是可打断的。例如，若左边框折叠时，刚刚向左滑动100px后就需要重新展开，那么展开的动画就从向左100px的位置开始，而不是从向左340px的位置开始（左margin，元素缩放和旋转同理）。

而展开左边框就是折叠左边框的反向动画。

最后，给左边框的右上角加一个按钮（按钮的素材在\static\images\menu.png）（按钮的宽高均为30px，与左边框的右侧和上侧分别距离15px），用户点击后，左边框就折叠；折叠状态下点击按钮，边框就展开。
按钮在折叠的时候也有动画：展开时正常；折叠的时候需要逆时针旋转180°（展开的时候在顺时针旋转180°恢复正常）。

如果你看懂了，就输出你的方案。
回复格式用 Markdown 分节，待我审核后才允许更改代码！
必须做的事情：
- 遵守现在的OOP代码风格，以传参的形式获取其他模块的实例
- 使用Web Animation API，才能更好地制作可打断动画


伪代码：

when the width of window drops below 1200px
    if no animation is played
        if sidebar is not collapsed
            play animation of collapsing sidebar
            play animation of extending content
            (extending content means to decrease its margin-left)
        end if
    else
        if sidebar is collapsing
            pass
        else
            play animation of collapsing sidebar from current position
            play animation of extending content from current position
        end if
    end if
end when

when the width of window exceeds 1200px
    if user have collapsed side bar
        return
    end if

    if no animation is played
        play animation of squeezing content
        (squeezing content means to increase its margin-left)
        if sidebar is not collapsed and is in the floating mode
            play animation of removing mask
            forget that user have collapsed the sidebar
        else
            play animation of extending sidebar
        end if
    else
        play animation of squeezing content from current position
        if sidebar is extending, entering into the floating mode
            play animation of removing mask from current position
            forget that user have collapsed the sidebar
        else if sidebar is collapsing
            play animation of extending sidebar from current position
        end if
    end if

end when

when user clicks the button on the topright corner, or when user clicks the existing mask:
    if sidebar is collapsed or is collapsing
        if the width of window > 1200px
            play animation of extending sidebar from current position
            play animation of squeezing content from current position
        else
            play animation of extending sidebar from current position
            play animation of appending mask from current position
        end if

        if the width of window > 1200px
            forget that user have collapsed the sidebar
        end if
    else
        play animation of collapsing sidebar from current position
        if mask exists
            play animation of removing mask from current position
        else
            play animation of extending content from current position
        end if

        if the width of window > 1200px
            remember that user have collapsed the sidebar
        end if
    end if
end when

请你加入一个新功能：

当input页面的单词列表变化时（加入、删除、清空），通知打开的passage-input窗口（如果有）。
passage-input窗口将文章中存在于input词表的单词渲染成用户悬浮该单词相同的效果，除了背景颜色变为淡黄色。
当然，不再存在于input词表的，但仍然是淡黄色的单词，恢复正常视觉效果。

如果你看懂了，就输出你的方案。
回复格式用 Markdown 分节，待我审核后才允许更改代码！
必须做的事情：
- 遵守现在的OOP代码风格（依赖注入）
- 使用十六进制颜色时，请用括号描述其颜色！例如#2BC016（鲜艳的绿色）
